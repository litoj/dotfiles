#!/usr/bin/bash

TEXT=$(date +"%S")
((TEXT > 0)) && sleep $((60 - TEXT))
date +" %H:%M"
while sleep 60; do
	date +" %H:%M"
done

#~/.config/i3blocks/colorcode $((($(date +%H) * 3600 + $(date +%M)) / 432))
