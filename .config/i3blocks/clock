#!/usr/bin/env bash
# Clock script
if ((button == 1)); then
	date +" %H:%M:%S"
	date +" %H:%M:%S"
else
	date +" %H:%M"
	date +" %H:%M"
fi
~/.config/i3blocks/colorcode $((($(date +%H) * 3600 + $(date +%M)) / 432))

(($(date +"%H") > 19)) && xrandr --output eDP --gamma 1:0.9:0.75 || exit 0
